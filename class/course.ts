import { Collection } from './collection';

class Course extends Collection {
    constructor (metaData) {
        super(metaData);
    }
}
// const course = new Course({});
// const trackable = course.isTrackableCollection();